<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_centerVertical="true"
        android:layout_marginBottom="20dp">

        <android.support.design.widget.TabLayout
            android:id="@+id/tl_fm"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:tabBackground="@drawable/tab_selector"
            app:tabIndicatorColor="@color/tab_bg"
            app:tabIndicatorHeight="0dp"
            app:tabMinWidth="96dp"
            app:tabMode="scrollable"
            app:tabSelectedTextColor="@color/white"
            app:tabTextAppearance="@style/tab_text_size"
            app:tabTextColor="@color/black_title" />
    </LinearLayout>

    <com.example.p010_recycleviewall.widget.NoScrollViewPager.NoScrollViewPager
        android:id="@+id/vp_fm"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</LinearLayout>